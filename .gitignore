# ── DB / 데이터 ────────────────────────────────────
data/
*.db
*.db-shm
*.db-wal
*.db-journal

# ── 파이썬 캐시 ────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# ── 빌드 산출물 ────────────────────────────────────
dist/
build/
*.spec
GongmoRadar.exe

# ── 설정 백업 / 임시 파일 ──────────────────────────
*.bak
*.tmp
*.log
logs/

# ── 환경 변수 / 비밀 ──────────────────────────────
.env
.env.*

# ── 운영 설정 (API 키·이메일·텔레그램 토큰 포함) ──────────────
# config.example.yaml 만 공개 저장소에 올린다.
# 실제 운영 파일은 로컬에서만 관리한다.
config.yaml
window_state.json

# ── IDE ───────────────────────────────────────────
.vscode/
.idea/
*.swp

# 자동 업데이트 임시 파일
*.new
version.txt.new
_gongmo_update.bat


# 시스템 생성 디렉토리
NVIDIA Corporation/
umdlogs/

# 평가 보고서 (내부 참고용, 배포 불필요)
*평가*.md
*가이드라인*.md
